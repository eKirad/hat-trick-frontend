// React imports
import React from 'react';

// Component imports

// Service imports

interface IProps { };

const MainView: React.FC<IProps> = (props) => {
    return(<h1>Hi from MainView!!!</h1>)
};

export default MainView;
